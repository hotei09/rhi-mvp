// 글로벌 로딩 placeholder — Phase 1+ 에서 skeleton 컴포넌트로 교체
export default function Loading() {
  return (
    <div className="flex min-h-screen items-center justify-center text-sm text-muted-foreground">
      Loading...
    </div>
  );
}
